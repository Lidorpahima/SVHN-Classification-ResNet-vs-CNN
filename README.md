# SVHN-Classification-ResNet-vs-CNN
repository explores the performance of a pre-trained **ResNet50** model using transfer learning compared to a custom-built **CNN** for classifying numbers in the **SVHN dataset**. 
